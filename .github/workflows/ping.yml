name: Ping Website

on:
  schedule:
    - cron: '*/55 * * * *'  # Every 59 minutes UTC
  workflow_dispatch:        # Allows manual runs from GitHub

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your site
        run: curl -s https://yourwebsite.com || echo "Ping failed"
